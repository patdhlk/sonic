# artefact-catalog.yaml — generated by pharaoh-setup
#
# Per-type policy: which fields are required, which links are required,
# which lifecycle applies. Pharaoh's MECE / coverage / completeness checks
# read this file. Edit as governance policy stabilises.

feat:
  required_fields: [id, status, title]
  optional_fields: [reviewer, approved_by, source_doc]
  lifecycle: [open, draft, reviewed, approved]
  required_links: []
  optional_links: [satisfies, refines, implements, verifies]
  required_metadata_fields: [status]
  required_roles: []

req:
  required_fields: [id, status, title]
  optional_fields: [reviewer, approved_by, source_doc]
  lifecycle: [open, draft, reviewed, approved]
  required_links: [satisfies]
  optional_links: [refines, implements, verifies]
  required_metadata_fields: [status]
  required_roles: []

spec:
  required_fields: [id, status, title]
  optional_fields: [reviewer, approved_by, source_doc]
  lifecycle: [open, draft, reviewed, approved]
  required_links: [refines]
  optional_links: [satisfies, implements, verifies]
  required_metadata_fields: [status]
  required_roles: []

impl:
  required_fields: [id, status, title]
  optional_fields: [reviewer, approved_by, source_doc]
  lifecycle: [open, draft, reviewed, approved]
  required_links: [implements]
  optional_links: [satisfies, refines, verifies]
  required_metadata_fields: [status]
  required_roles: []

test:
  required_fields: [id, status, title]
  optional_fields: [reviewer, approved_by, source_doc]
  lifecycle: [open, draft, reviewed, approved]
  required_links: [verifies]
  optional_links: [satisfies, refines, implements]
  required_metadata_fields: [status]
  required_roles: []
