# workflows.yaml — generated by pharaoh-setup
#
# Observed status counts in spec/**/*.rst:
#   open: 3
#
# Only one distinct value observed; appending Pharaoh-default lifecycle
# states so transitions are at least defined. Edit to match project
# policy as the catalogue matures.

lifecycle_states:
  - open
  - draft
  - reviewed
  - approved

# Permissive forward chain. The histogram does not record transitions —
# adjust this graph to match real project policy (e.g. add reviewed → open
# for rework, approved → closed for end-of-life).
transitions:
  - {from: open, to: draft, requires: []}
  - {from: draft, to: reviewed, requires: []}
  - {from: reviewed, to: approved, requires: []}
